{{- $root := . -}}
{{- range $chart := .Values.charts }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: {{ $chart }}
  namespace: flux-system
spec:
  interval: 1m
  chartRef:
    kind: OCIRepository
    name: {{ $chart }}
    namespace: flux-system
---
{{- end }}
