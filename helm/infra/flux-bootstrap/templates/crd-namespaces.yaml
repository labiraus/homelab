{{- $seen := dict -}}
{{- range $r := .Values.crdReleases }}
{{- $ns := $r.targetNamespace | default $r.name -}}
{{- if not (hasKey $seen $ns) -}}
{{- $_ := set $seen $ns true -}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $ns }}
---
{{- end }}
{{- end }}
