{{- $ingress := .Values.ingress | default (dict) -}}
{{- $ingressEnabled := (index $ingress "enabled") | default false -}}
{{- $ingressHosts := (index $ingress "hosts") | default (list) -}}
{{- if $ingressEnabled -}}
{{- $root := . -}}
{{- range $host := $ingressHosts }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "flagger.fullname" $root }}
  namespace: flagger-system
spec:
  hostnames: [{{ $host.host }}]
  parentRefs:
  - name: gateway
    namespace: ingress
  rules:
  {{- range $path := $host.paths }}
  - matches:
    - path:
        type: {{ $path.pathType }}
        value: {{ $path.path }}
    filters:
      - type: URLRewrite
        urlRewrite:
          path:
            type: ReplacePrefixMatch
            replacePrefixMatch: /
    backendRefs:
    - name: {{ include "flagger.fullname" $root }}
      kind: Service
      port: 80
  {{- end }}
---
{{- end }}
{{- end }}
