service:
  type: ClusterIP
  port: 3000
ingress:
  enabled: true
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: local.homelab.com
      paths:
        - path: /grafana
          pathType: PathPrefix
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - local.homelab.com
