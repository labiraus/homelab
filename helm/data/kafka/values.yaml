enabled: false
namespace: data
name: homelab-kafka
bootstrapAlias:
  enabled: true
  name: homelab-kafka
kafka:
  replicas: 3
  storageClass: longhorn
  storageSize: 50Gi
zookeeper:
  replicas: 3
  storageClass: longhorn
  storageSize: 20Gi
