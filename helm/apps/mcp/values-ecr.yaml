podLabels: {}

image:
  tag: "uat"

ingress:
  enabled: true
  className: ""
  annotations: {}
  hosts:
    - host: uat.labiraus.com
      paths:
        - path: /mcp
          pathType: PathPrefix
        - path: /.well-known/mcp.json
          pathType: Exact
        - path: /.well-known/oauth-protected-resource
          pathType: Exact

tolerations:
  - key: "karpenter.sh/controller"
    operator: "Equal"
    value: "true"
    effect: "NoSchedule"

env:
  configValue: "eks"
