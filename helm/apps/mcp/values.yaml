# Default values for mcp.

replicaCount: 1
minReadySeconds: 5
terminationGracePeriodSeconds: 30

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 1

readinessProbe:
  httpGet:
    path: /readiness
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 2
  failureThreshold: 6

livenessProbe:
  httpGet:
    path: /liveness
    port: 8080
  initialDelaySeconds: 15
  periodSeconds: 10
  timeoutSeconds: 2
  failureThreshold: 3

lifecycle:
  preStop:
    exec:
      command:
        - /bin/sh
        - -c
        - sleep 5

namespace: homelab

image:
  repository: mcp
  pullPolicy: IfNotPresent
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

envFromSecrets:
  - mcp-data-connections

generatedSecrets:
  - name: mcp-data-connections
    stringData:
      NATS_URLS:
        value: nats://nats-nats.nats.svc.cluster.local:4222
      NATS_EVENTS_STREAM:
        value: document-events
      NATS_EVENTS_SUBJECT:
        value: documents.events.>
      POSTGRES_HOST:
        value: app-db-rw.data.svc.cluster.local
      POSTGRES_PORT:
        value: "5432"
      POSTGRES_DATABASE:
        value: app
      POSTGRES_USER:
        fromSecretRef:
          namespace: data
          name: app-db-bootstrap
          key: username
      POSTGRES_PASSWORD:
        fromSecretRef:
          namespace: data
          name: app-db-bootstrap
          key: password
      EMBEDDING_ENDPOINT:
        value: ""
      EMBEDDING_MODEL:
        value: local-embeddings
      MINIO_ENDPOINT:
        value: "svartalfheim:9000"
      MINIO_USE_SSL:
        value: "false"
      MINIO_BUCKET:
        value: "documents"
      MINIO_ACCESS_KEY:
        fromSecretRef:
          namespace: homelab
          name: documents-minio-credentials
          key: accessKey
      MINIO_SECRET_KEY:
        fromSecretRef:
          namespace: homelab
          name: documents-minio-credentials
          key: secretKey

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

security:
  istio:
    peerAuthentication:
      enabled: true
      mode: STRICT
    authorizationPolicy:
      enabled: true
      action: ALLOW
      rules:
        - from:
            - source:
                principals:
                  - cluster.local/ns/ingress/sa/gateway-istio
          to:
            - operation:
                ports: ["8080"]
  networkPolicy:
    enabled: true
    defaultDenyIngress: true
    defaultDenyEgress: true
    ingress:
      - from:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: ingress
        ports:
          - protocol: TCP
            port: 8080
    egress:
      - to:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: kube-system
            podSelector:
              matchLabels:
                k8s-app: kube-dns
        ports:
          - protocol: UDP
            port: 53
          - protocol: TCP
            port: 53
      - to:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: istio-system
            podSelector:
              matchLabels:
                app: istiod
        ports:
          - protocol: TCP
            port: 15012
      - to:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: nats
        ports:
          - protocol: TCP
            port: 4222
      - to:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: data
            podSelector:
              matchLabels:
                cnpg.io/cluster: app-db
        ports:
          - protocol: TCP
            port: 5432
      - to:
          - namespaceSelector:
              matchLabels:
                kubernetes.io/metadata.name: homelab
        ports:
          - protocol: TCP
            port: 80
      - to:
          - ipBlock:
              cidr: 0.0.0.0/0
        ports:
          - protocol: TCP
            port: 9000

podAnnotations: {}
podLabels:
  testing_label_key: mcp

podSecurityContext: {}
securityContext: {}

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  httpsRedirect: true
  className: ""
  annotations: {}
  hosts:
    - host: mcp.labiraus.com
      paths:
        - path: /mcp
          pathType: PathPrefix
        - path: /sse
          pathType: Exact
        - path: /messages
          pathType: Exact
        - path: /message
          pathType: Exact
        - path: /.well-known/mcp.json
          pathType: Exact
        - path: /.well-known/oauth-protected-resource
          pathType: Exact
        - path: /.well-known/oauth-protected-resource/mcp
          pathType: Exact
  tls: []

resources: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity: {}

env:
  configValue: "default"
  API_BASE_URL: "http://homelab-orchestrator.homelab.svc.cluster.local"
  OIDC_ISSUER_URL: "https://accounts.google.com"
  # POSTGRES_SSLMODE: "disable"
  # MINIO_REGION: ""
