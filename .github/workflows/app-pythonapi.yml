name: App PythonApi CI/CD
on:
  repository_dispatch:
    types: [docker-build]
  push:
    paths:
      - "apps/pythoapi/**"
      - "apps/pkg/**"
      - ".github/actions/docker/**"
      - ".github/workflows/app-pythoapi.yml"

# This section defines the permissions given to the workflow through the secrets.GITHUB_TOKEN
permissions:
  contents: read
  packages: write
  id-token: write

concurrency: ci-pythoapi-docker-pipeline

jobs:
  test:
    name: "PythonApi Test"
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci')

    steps:
      -
        name: Checkout
        uses: actions/checkout@v4.1.1

      - 
        name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: '1.22.4' # Specify the Go version to use. Default is the latest stable version

      - 
        name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('./apps/pythoapi/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-
    
      -
        name: unit test
        run: go test ./apps/pythoapi/...

  ghcr:
    name: "PythonApi Build to GHCR"
    runs-on: ubuntu-latest
    environment: uat
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') && !contains(github.event.head_commit.message, 'no-ci')

    steps:
      -
        name: Checkout
        uses: actions/checkout@v4.1.1
    
      - name: Build to GHCR
        uses: ./.github/actions/docker/ghcr
        with:
          dockerfile: ./apps/pythoapi/dockerfile
          context: ./apps
          token: ${{ secrets.GITHUB_TOKEN }}
          registry: pythoapi
          tag: latest