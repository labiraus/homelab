name: Helm ReactApp CI/CD
on:
  repository_dispatch:
    types: [helm-build]
  push:
    paths:
      - helm/apps/pythonapi/**/*
      - ".github/actions/helm/**"
      - ".github/workflows/helm-reactapp.yml"

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  test-default:
    name: "ReactApp Helm Test"
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Test Default
        uses: ./.github/actions/helm/test
        with:
          chart: apps/pythonapi
          values: values.yaml

  test-ghcr:
    name: "ReactApp Helm Test GHCR"
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Test GHCR
        uses: ./.github/actions/helm/test
        with:
          chart: apps/pythonapi
          values: values-ghcr.yaml

  test-ecr:
    name: "ReactApp Helm Test ECR"
    runs-on: ubuntu-latest
    environment: uat
    if: github.ref != 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci') && (vars.USE_ECR == 'true' || vars.USE_ECR == '')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Test ECR
        uses: ./.github/actions/helm/test
        with:
          chart: apps/pythonapi
          values: values-ecr.yaml

  ghcr:
    name: "ReactApp Helm Build to GHCR"
    runs-on: ubuntu-latest
    environment: uat
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') && !contains(github.event.head_commit.message, 'no-ci')

    steps:
      - 
        name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - 
        name: Build to GHCR
        uses: ./.github/actions/helm/ghcr
        with:
          chart: apps/pythonapi
          token: ${{ secrets.GITHUB_TOKEN }}
          values: values-ghcr.yaml
          tag: uat

  ecr:
    name: "ReactApp Helm Build to ECR"
    runs-on: ubuntu-latest
    environment: uat
    if: github.ref == 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci') && (vars.USE_ECR == 'true' || vars.USE_ECR == '')
    steps:
      - 
        name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Build to ECR
        uses: ./.github/actions/helm/ecr
        with:
          chart: apps/pythonapi
          aws-region: ${{ vars.AWS_REGION}}
          role-to-assume: ${{ vars.OIDC_BUILD_ROLE}}
          registry: k8s-quickstart-ecr/helm-reactapp
          image_repository: k8s-quickstart-ecr/reactapp
          values: values-ecr.yaml
          tag: uat
