name: Helm Namespace CI/CD
on:
  repository_dispatch:
    types: [helm-build]
  push:
    paths:
      - helm/infra/namespace/**/*
      - ".github/actions/helm/**"
      - ".github/workflows/helm-namespace.yml"

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  test-default:
    name: "Namespace Helm Test"
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Test Default
        uses: ./.github/actions/helm/test
        with:
          chart: infra/namespace
          values: values.yaml

  test-ghcr:
    name: "Namespace Helm Test GHCR"
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Test GHCR
        uses: ./.github/actions/helm/test
        with:
          chart: infra/namespace
          values: values-ghcr.yaml

  test-ecr:
    name: "Namespace Helm Test ECR"
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci') && vars.USE_ECR != 'false'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Test ECR
        uses: ./.github/actions/helm/test
        with:
          chart: infra/namespace
          values: values-ecr.yaml

  ghcr:
    name: "Namespace Helm Build to GHCR"
    runs-on: ubuntu-latest
    environment: uat
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') && !contains(github.event.head_commit.message, 'no-ci')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Build to GHCR
        uses: ./.github/actions/helm/ghcr
        with:
          chart: infra/namespace
          token: ${{ secrets.GITHUB_TOKEN }}
          values: values-ghcr.yaml
          tag: uat

  ecr:
    name: "Namespace Helm Build to ECR"
    runs-on: ubuntu-latest
    environment: uat
    if: github.ref == 'refs/heads/main' && !contains(github.event.head_commit.message, 'no-ci') && vars.USE_ECR != 'false'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.1.1
    
      - name: Build to ECR
        uses: ./.github/actions/helm/ecr
        with:
          chart: infra/namespace
          aws-region: ${{ vars.AWS_REGION}}
          role-to-assume: ${{ vars.OIDC_BUILD_ROLE}}
          registry: k8s-quickstart-ecr/helm-namespace
          values: values-ecr.yaml
          tag: uat
