namespace: mongodb
cluster:
  name: mongo-single
  members: 1
  version: "7.0.12"
  storageClassName: longhorn
  storageSize: 20Gi
  appUser: app
  appDatabase: app
  passwordSecretName: mongo-app-user-password
  scramSecretName: mongo-app-scram
