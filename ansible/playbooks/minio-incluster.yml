---
- name: Configure in-cluster MinIO buckets, users, policies, lifecycle
  hosts: minio_incluster
  gather_facts: false
  roles:
    - role: minio_bootstrap
    - role: minio_buckets
    - role: minio_policies
    - role: minio_users
    - role: minio_replication
