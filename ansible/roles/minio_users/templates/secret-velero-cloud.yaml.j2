apiVersion: v1
kind: Secret
metadata:
  name: {{ secret_name }}
  namespace: {{ secret_namespace }}
type: Opaque
stringData:
  cloud: |
    [default]
    aws_access_key_id={{ access_key }}
    aws_secret_access_key={{ secret_key }}
